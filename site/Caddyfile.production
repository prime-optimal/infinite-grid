# This should be set to the domain when it's online, but localhost when testing locally.
sf6.lol {

reverse_proxy localhost:8080
encode gzip
templates
file_server browse
}

:8181 {
respond "port 8181"
file_server browse
}
